# Keeps the SHA-pinned actions in .github/workflows/ up to date: Dependabot
# opens a PR whenever an action publishes a new release, bumping the pin.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
